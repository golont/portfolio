// import gsap from "gsap";

// const elements = document.querySelectorAll(".rotate");

// for (const element of elements) {
//   console.log(element);
//   gsap.to(element, { rotation: "360", ease: "linear", repeat: "-1" });
// }
